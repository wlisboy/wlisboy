使用方式
```
# 参数
-f FILE, --file FILE  存放待测试 IP 列表的文本文件路径 (.txt)
-o OUTPUT, --output OUTPUT  导出的报表文件路径 (.csv 或 .txt)

# 示例                        
py nexttrace.py -f ip.txt -o result.csv
py nexttrace.py -f ip.txt -o result.txt
```
